/* DevOps-Infra Learn — course data. window.CHAPTERS = 8 chapters x 5 levels each. */
window.CHAPTERS = [

{ num: 1, emoji: '⚙️', title: 'IaC Fundamentals — infrastructure as text', layer: 'Foundation',
  tagline: 'Why we stopped SSH-ing into servers. Desired state, drift, idempotency, and the state file that runs (and ruins) your day.',
  apps: ['🏦 bank deploys', '🛒 Black Friday scale-outs', '🔁 disaster recovery'],
  levels: [
  { html: '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Pets vs cattle. A pet server has a name, you hand-feed it, and when it gets sick you sit up all night nursing it. Cattle have tag numbers: if one is sick you replace it. Infrastructure as Code (IaC) turns pets into cattle — the server is described in a file, and any copy built from that file is identical.</p></div>' +
    '<p><b>IaC</b> is writing your servers, networks, databases and DNS as text files, checked into git, and applied by a tool. The file is the single source of truth; the real world is made to match it.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>When a teammate says <i>"it works on staging but not prod"</i>, IaC is the fix: prod and staging are built from the same code, so they cannot silently drift apart. When prod melts, you <code>apply</code> the code to a fresh region and you are back.</p></div>',
    try: [['📖 Read: IaC Beginner Guide', '../iac/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p><b>Declarative</b> beats <b>imperative</b>. Imperative = a script of steps ("install nginx, then edit this file, then restart"). Declarative = a description of the end state ("nginx is installed and running with this config"). The tool figures out the steps, and running it twice changes nothing the second time — that property is <b>idempotency</b>.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>A thermostat, not a to-do list. You do not tell the furnace "burn for 8 minutes"; you say "21°C" and the system closes the gap, forever, whatever the weather.</p></div>' +
    '<pre><code>imperative:  create_vm; attach_disk; open_port_443   # what if step 2 already ran?\ndeclarative: vm { disk = 50; ports = [443] }          # converge to this, always</code></pre>',
    try: [['📖 Declarative vs imperative (deep dive)', '../iac/DEEP_DIVE.md', 'o']] },
  { html: '<p>The tool needs a memory of what it built — the <b>state</b>. Terraform writes a <code>terraform.tfstate</code> JSON file mapping "resource in my code" ↔ "real thing in the cloud". Without it, the tool cannot tell <i>create a new one</i> from <i>update the existing one</i>.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Delete the state file and Terraform forgets your database exists — next <code>apply</code> tries to build a second one. Commit it to git and you have just published your DB password in plaintext. This is why real teams use a <b>remote backend</b> (S3/GCS + a lock).</p></div>' +
    '<pre><code>desired (code)  :  server "web" { size = "large" }\nstate (memory)  :  web -> i-0abc123, size = "small"\nreal world      :  i-0abc123 is "small"\n=> plan: resize i-0abc123 small -> large</code></pre>',
    try: [['🔬 Lab: watch state appear', '../terraform/examples/01-hello/main.tf', 'o']] },
  { html: '<p><b>Drift</b> is when reality stops matching the code — someone clicks in the console, an autoscaler adds a rule, an incident hotfix never got back-ported. <code>terraform plan</code> is a drift detector: it diffs code vs state vs real world and shows you the gap before you touch anything.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Drift is edits made to the printed copy while the master document sits untouched. <code>plan</code> is track-changes: it shows every unauthorised scribble so you decide — adopt it into the master, or overwrite it.</p></div>',
    try: [['📖 Drift & reconciliation', '../iac/DEEP_DIVE.md', 'o']] },
  { html: '<p>Expert view: IaC splits into <b>provisioning</b> (Terraform, Pulumi, CloudFormation — create cloud resources) and <b>configuration management</b> (Ansible, Chef — configure inside a machine), though <b>immutable infrastructure</b> (bake an image, never mutate it, replace to change) is eating the second category. Layer on <b>policy as code</b> (OPA/Conftest, Sentinel) so "no public S3 buckets" is enforced in CI, and <b>GitOps</b> (Argo CD, Flux) so a git merge <i>is</i> the deploy.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>A modern platform: PR opens → <code>plan</code> + policy check comment on the PR → merge → pipeline runs <code>apply</code> → drift job re-plans nightly and alerts. Nobody has console write access.</p></div>',
    try: [['📖 GitOps & policy as code', '../iac/DEEP_DIVE.md', 'o']] } ],
  quiz: [
    { q: 'What makes an operation "idempotent"?', opts: ['It runs fast', 'Running it again produces no further change once the desired state is reached', 'It never fails', 'It only runs once ever'], ok: 1, why: 'Declarative tools converge to a described state; a second apply with the same code is a no-op. That is what lets you run them safely on a schedule.' },
    { q: 'Why is the Terraform state file dangerous to mishandle?', opts: ['It is very large', 'It maps code to real resources AND stores resource attributes (often secrets) in plaintext — lose it and the tool forgets; commit it and you leak', 'It slows down apply', 'It is written in binary'], ok: 1, why: 'No state = Terraform cannot map code to reality (it will recreate things). State in git = plaintext secrets in history. Hence remote, encrypted, locked backends.' },
    { q: 'A colleague hotfixed a security-group rule in the AWS console. What surfaces it?', opts: ['Nothing, it is lost', 'terraform plan — it diffs code vs state vs the live world and shows the drift', 'The state file auto-updates silently', 'Only a full redeploy'], ok: 1, why: 'plan is a drift detector. You then choose: codify the change, or let apply revert it. Nightly drift plans catch this automatically.' } ]
},

{ num: 2, emoji: '🏗️', title: 'Terraform — the plan/apply lifecycle', layer: 'Foundation',
  tagline: 'Providers, the state file up close, modules as functions, workspaces, and the migrations that keep teams awake.',
  apps: ['☁️ multi-cloud estates', '🏗️ platform teams', '🔐 change review in PRs'],
  levels: [
  { html: '<p>Terraform has one loop: <b>write</b> HCL → <code>init</code> (download providers) → <code>plan</code> (compute the diff) → <code>apply</code> (make it real) → <code>destroy</code> (tear it down). The <b>plan is the review artifact</b> — never apply what you have not read.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p><code>plan</code> is the contractor\'s quote: "I will knock down this wall, keep the kitchen, add two sockets." You approve the quote (<code>apply</code>), not the vague idea of a renovation.</p></div>' +
    '<pre><code>terraform init      # wire up providers + backend\nterraform plan      # + create 3, ~ update 1, - destroy 0\nterraform apply     # do exactly that, then update state\nterraform destroy   # remove everything this code owns</code></pre>',
    try: [['🔬 Lab: 01-hello', '../terraform/examples/01-hello/main.tf', 'o']] },
  { html: '<p><b>Providers</b> are plugins that translate HCL into API calls — <code>aws</code>, <code>google</code>, <code>kubernetes</code>, <code>github</code>, <code>cloudflare</code>, hundreds more. You pin their versions (<code>~> 5.0</code>) so a colleague\'s <code>init</code> next month behaves like yours today.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>One stack often uses several providers at once: <code>aws</code> to make the cluster, <code>kubernetes</code>/<code>helm</code> to deploy onto it, <code>github</code> to wire the CI secret. Terraform builds a dependency graph across all of them.</p></div>',
    try: [['📖 Terraform Beginner Guide', '../terraform/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p>A <b>module</b> is a folder of <code>.tf</code> you call like a function: inputs (<code>variable</code>), body (resources), outputs (<code>output</code>). Call it once or, with <code>for_each</code>, many times. Good modules have a small surface, validated inputs, and sane defaults.</p>' +
    '<pre><code>module "service" {\n  source   = "./modules/service"\n  for_each = { api = 3, worker = 2, cron = 1 }\n  name     = each.key\n  replicas = each.value\n}</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>A module is an IKEA flat-pack: the instructions (module code) are written once; you build the same wardrobe in ten bedrooms by changing only the colour and size on the box (inputs).</p></div>',
    try: [['🔬 Lab: 03-module (for_each)', '../terraform/examples/03-module/main.tf', 'o']] },
  { html: '<p>Real teams never keep state on a laptop. A <b>remote backend</b> (S3 + DynamoDB lock, GCS, Terraform Cloud) gives you sharing, <b>locking</b> (two applies cannot race and corrupt state), versioning (undo), and encryption. <b>Workspaces</b> or, better, <b>separate state per environment</b> keep dev and prod blast radius apart.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p><code>Error: state blob is already locked</code> — someone\'s <code>apply</code> crashed and left the lock. You verify nobody is really running, then <code>terraform force-unlock &lt;id&gt;</code>. Doing that while a real apply runs is how state gets corrupted.</p></div>',
    try: [['🔬 Lab: 04-remote-state', '../terraform/examples/04-remote-state/backend.tf', 'o']] },
  { html: '<p>Expert view: the hard parts are <b>refactors without downtime</b>. <code>terraform import</code> adopts a hand-made resource into state; the <code>moved {}</code> block renames a resource in code without destroy/recreate; <code>terraform state mv</code> does it manually; <code>-target</code> and <code>-replace</code> scalpel a single resource. Also: <b>drift automation</b>, <b>Terragrunt</b> for DRY backends, and <b>Atlantis/Spacelift/TFC</b> for plan-in-PR workflows.</p>' +
    '<pre><code>moved {\n  from = aws_instance.web\n  to   = aws_instance.frontend   # rename, no downtime\n}</code></pre>',
    try: [['📖 Terraform prod scenarios', '../terraform/SCENARIOS.md', 'o']] } ],
  quiz: [
    { q: 'In the Terraform workflow, what is the artifact you actually review?', opts: ['The apply logs', 'The plan — the exact create/update/destroy diff, approved before apply', 'The state file', 'The provider version'], ok: 1, why: 'plan shows precisely what will change. Reviewing apply output is too late; reviewing the code alone misses drift and computed values.' },
    { q: 'Why do teams use a remote backend with locking?', opts: ['It is faster', 'Shared state, plus a lock so two concurrent applies cannot corrupt it — with versioning and encryption', 'It avoids writing HCL', 'It removes the need for providers'], ok: 1, why: 'Local state breaks the moment a second person or CI runs apply. Locking (DynamoDB, native) serialises applies; versioning gives you undo.' },
    { q: 'You renamed a resource in code and plan shows destroy + create of your database. Best fix?', opts: ['Apply it, databases are cheap', 'Add a moved {} block (or terraform state mv) so Terraform treats it as a rename, not a replace', 'Delete the state file', 'Use -target to skip it'], ok: 1, why: 'moved {} / state mv update the address in state so no real resource is destroyed. Applying the replace would drop the data.' } ]
},

{ num: 3, emoji: '🐳', title: 'Docker & Containers — a process in a costume', layer: 'Systems',
  tagline: 'What a container really is, image layers and the build cache, multi-stage and distroless, networking, volumes, and Compose.',
  apps: ['💻 "works on my machine" gone', '🚀 CI build pipelines', '📦 every K8s Pod'],
  levels: [
  { html: '<p>A container is <b>just a Linux process</b> with a restricted view. <b>Namespaces</b> lie to it about what it can see (its own PID 1, its own network, its own filesystem root); <b>cgroups</b> cap what it can use (CPU, memory). No hypervisor, no guest OS — that is why it starts in milliseconds.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>A hotel room, not a house. You get a private door, bed and bathroom (namespaces) and a metered power socket (cgroups), but you share the building\'s foundation and plumbing — the host kernel. A VM is a separate house with its own foundation.</p></div>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p><i>"Works on my machine"</i> dies here: the image ships the app <b>and</b> its libc, its Python, its CA certs. The host only provides the kernel.</p></div>',
    try: [['📖 Docker Beginner Guide', '../docker/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p>An <b>image</b> is a stack of read-only <b>layers</b>, one per build instruction, content-addressed by hash. Containers add a thin writable layer on top. Layers are cached and shared: pull a new image and Docker only downloads layers it does not already have.</p>' +
    '<pre><code>FROM python:3.12-slim      # layer A (shared by everything)\nCOPY requirements.txt .    # layer B — changes rarely\nRUN pip install -r ...     # layer C — cached until B changes\nCOPY . .                   # layer D — changes every commit</code></pre>' +
    '<p>Order matters: put the things that change least at the top. Swap <code>COPY . .</code> above the <code>pip install</code> and every code edit re-installs every dependency.</p>',
    try: [['📖 Layers & the build cache', '../docker/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p><b>Multi-stage builds</b>: use a fat image with compilers to build, then <code>COPY --from=build</code> only the artifact into a tiny runtime image. <b>Distroless</b> / <code>scratch</code> go further — no shell, no package manager, nothing to exploit.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Cook in a full commercial kitchen (build stage), then send only the plated dish to the table (runtime stage). The diners do not need your ovens — or the knives.</p></div>' +
    '<pre><code>FROM golang:1.23 AS build\nRUN go build -o /app .\n\nFROM gcr.io/distroless/static AS runtime\nCOPY --from=build /app /app\nUSER 65532\nENTRYPOINT ["/app"]      # ~10 MB, no shell, non-root</code></pre>',
    try: [['🔬 Lab: multi-stage Dockerfile', 'https://github.com/satyabhan007/DevOps-Infra/blob/main/docker/examples/multistage/Dockerfile', 'o']] },
  { html: '<p><b>PID 1 matters.</b> The first process in a container is the init process: it must forward signals and reap zombies. If your <code>CMD</code> is a shell script that runs the app with <code>&</code>, <code>SIGTERM</code> never reaches the app and the orchestrator kills it after the grace period. Use exec-form (<code>ENTRYPOINT ["app"]</code>) or a tiny init (<code>tini</code>).</p>' +
    '<p><b>Volumes</b> persist data past the container\'s life; <b>bind mounts</b> map a host path in (great for dev). <b>Networking</b>: containers on the same user-defined network reach each other by name; publish a port (<code>-p 8080:80</code>) to expose one to the host.</p>',
    try: [['📖 Docker prod scenarios', '../docker/SCENARIOS.md', 'o']] },
  { html: '<p>Expert view: <b>BuildKit</b> (cache mounts, <code>--mount=type=secret</code> so secrets never land in a layer, parallel stages), <b>multi-arch</b> builds with <code>buildx</code> (amd64 + arm64 from one command), <b>SBOM + provenance</b> attestations for supply-chain audits, and image scanning (<code>trivy</code>, <code>docker scout</code>) in CI. <b>Compose</b> wires a whole local stack — app + db + cache — with health-gated <code>depends_on</code>.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>A leaked AWS key in image history is a classic: it was <code>COPY</code>-ed in, then deleted in a later layer — but the earlier layer still has it. <code>--mount=type=secret</code> or multi-stage prevents it.</p></div>',
    try: [['🔬 Lab: Compose stack', '../docker/examples/compose/docker-compose.yml', 'o']] } ],
  quiz: [
    { q: 'What is a container, mechanically?', opts: ['A lightweight virtual machine with its own kernel', 'A host process isolated by namespaces (what it sees) and limited by cgroups (what it uses) — sharing the host kernel', 'An emulator', 'A zip file that runs'], ok: 1, why: 'No guest kernel, no hypervisor. That shared-kernel design is why containers start in ms and also why kernel isolation (seccomp, non-root) matters.' },
    { q: 'Your Dockerfile re-installs all dependencies on every code change. Why?', opts: ['pip is slow', 'COPY . . sits ABOVE the dependency install, so its layer invalidates the install cache every commit — copy the manifest first', 'The base image is too big', 'You need --no-cache'], ok: 1, why: 'Layer cache invalidates from the first changed instruction downward. Copy requirements.txt and install before copying the app, so code edits reuse the deps layer.' },
    { q: 'Why prefer exec-form ENTRYPOINT ["app"] over a shell wrapper?', opts: ['It is shorter', 'The app becomes PID 1 and receives SIGTERM directly — shell-form / backgrounding swallows the signal and forces a kill', 'Shell form is deprecated', 'It runs faster'], ok: 1, why: 'Graceful shutdown needs the app to see SIGTERM. A shell that does not exec, or an app started with &, never gets it — the orchestrator then SIGKILLs after the grace period.' } ]
},

{ num: 4, emoji: '☸️', title: 'Kubernetes Architecture — the control loop', layer: 'Systems',
  tagline: 'Control plane vs nodes, the reconciliation pattern, and how a kubectl apply becomes a running Pod.',
  apps: ['🌐 most large web platforms', '🧩 internal developer platforms', '🔀 multi-region failover'],
  levels: [
  { html: '<p>Kubernetes is a <b>reconciliation loop</b>: you declare desired state (YAML), controllers continuously compare it to actual state and act to close the gap. Pod died? The loop makes a new one. That is the whole idea — everything else is detail.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Cruise control. You set 110 km/h (desired). The car reads speed (actual), and feeds gas or eases off, forever. It does not care <i>why</i> you slowed — a hill, a headwind — it just closes the gap.</p></div>',
    try: [['📖 Kubernetes Beginner Guide', '../kubernetes/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p>The <b>control plane</b>: <code>kube-apiserver</code> (the only thing that writes to <code>etcd</code>, the key-value store of all cluster state), <code>etcd</code> itself, <code>kube-scheduler</code> (assigns Pods to nodes), <code>controller-manager</code> (the reconcilers). The <b>nodes</b> run <code>kubelet</code> (talks to the container runtime, reports health) and <code>kube-proxy</code> (programs Service networking).</p>' +
    '<pre><code>kubectl apply ─▶ apiserver ─▶ etcd            (desired state stored)\n                   │\n     scheduler ◀────┘  picks a node\n     kubelet   ◀──────  sees "Pod for me", starts containers\n     kubelet   ──────▶  status back to apiserver</code></pre>',
    try: [['📖 K8s internals deep dive', '../kubernetes/DEEP_DIVE.md', 'o']] },
  { html: '<p>Walk a <code>kubectl apply -f deploy.yaml</code>: (1) apiserver validates + writes the Deployment to etcd; (2) the Deployment controller creates a ReplicaSet; (3) the ReplicaSet controller creates Pod objects; (4) the scheduler binds each Pod to a node by filtering (fits? tolerations?) then scoring (spread, least-loaded); (5) that node\'s kubelet pulls images and starts containers; (6) status flows back up.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p><code>kubectl get events --sort-by=.lastTimestamp</code> is your black-box recorder. "0/12 nodes available: 3 Insufficient cpu, 9 node(s) had taint" is the scheduler telling you exactly why a Pod is Pending.</p></div>',
    try: [['📖 K8s prod scenarios', '../kubernetes/PROD_SCENARIOS.md', 'o']] },
  { html: '<p><b>Everything is an object</b> with the same shape: <code>apiVersion</code>, <code>kind</code>, <code>metadata</code>, <code>spec</code> (desired), <code>status</code> (observed). Controllers own the gap between spec and status. <b>Labels</b> + <b>selectors</b> are the glue: a Service finds its Pods by label match, not by name or IP.</p>' +
    '<pre><code>Service.spec.selector: app=checkout\n        │  (label query, evaluated continuously)\n        ▼\nEndpoints: 10.1.2.3:8080, 10.1.2.9:8080, ...   # the Pods that match, right now</code></pre>',
    try: [['🔬 Lab: Deployment + Service', '../kubernetes/manifests/02-deployment-service.yaml', 'o']] },
  { html: '<p>Expert view: extend the API with <b>CRDs</b> + a custom <b>controller</b> (the Operator pattern — encode "how to run Postgres" as software). <b>Admission webhooks</b> (validating + mutating) enforce policy at write time — Kyverno, Gatekeeper, sidecar injection. The API is <b>eventually consistent</b> and <b>level-triggered</b>: controllers re-list and re-reconcile, so a missed event self-heals on the next sync.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>etcd is the crown jewel: it holds every Secret and the entire cluster state. Back it up, encrypt it at rest, and never let it fill its disk — a full etcd makes the whole API read-only.</p></div>',
    try: [['📖 CRDs, operators, admission', '../kubernetes/DEEP_DIVE.md', 'o']] } ],
  quiz: [
    { q: 'What is the core design pattern of Kubernetes?', opts: ['Message queues between microservices', 'A reconciliation loop: controllers continuously drive actual state toward declared desired state', 'A cron scheduler', 'A load balancer with health checks'], ok: 1, why: 'Every controller watches its objects and closes the spec/status gap. Self-healing, rollouts and scaling are all the same loop with different specs.' },
    { q: 'Which component actually writes cluster state, and where does it live?', opts: ['kubelet, on each node', 'kube-apiserver is the only writer; state lives in etcd', 'kube-scheduler, in memory', 'kube-proxy, in iptables'], ok: 1, why: 'All reads and writes go through the apiserver, which persists to etcd. That is why etcd backup/encryption is non-negotiable.' },
    { q: 'A Service keeps sending traffic to a dead Pod. Most likely cause?', opts: ['etcd is down', 'Label/selector or readiness mismatch — the Service selects by label and only Ready Pods become Endpoints', 'The scheduler is overloaded', 'DNS TTL'], ok: 1, why: 'Services route to Endpoints built from label-matching, Ready Pods. A failing readiness probe removes a Pod from rotation; a selector typo adds the wrong ones.' } ]
},

{ num: 5, emoji: '🚢', title: 'Kubernetes Workloads — Pods, Deployments, rollouts', layer: 'Platform',
  tagline: 'ReplicaSets and Deployments, StatefulSets and stable storage, Jobs, Services and Ingress routing.',
  apps: ['🟢 zero-downtime deploys', '🗄️ databases on K8s', '⏰ batch & cron jobs'],
  levels: [
  { html: '<p>You rarely create a <b>Pod</b> directly — no self-healing, no rollout. A <b>Deployment</b> manages a <b>ReplicaSet</b>, which keeps N identical Pods alive. Change the image and the Deployment rolls: new ReplicaSet up, old one down, gradually.</p>' +
    '<pre><code>kubectl set image deploy/checkout app=acme/checkout:v2\nkubectl rollout status deploy/checkout\nkubectl rollout undo   deploy/checkout   # back to v1 in seconds</code></pre>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Replacing a bus fleet while the route runs: roll in a new bus, let it pick up passengers (readiness), then retire an old one. Never park the whole fleet.</p></div>',
    try: [['🔬 Lab: Deployment + Service', '../kubernetes/manifests/02-deployment-service.yaml', 'o']] },
  { html: '<p><b>RollingUpdate</b> knobs: <code>maxUnavailable</code> (how far below target you will dip) and <code>maxSurge</code> (how far above). <code>maxUnavailable: 0, maxSurge: 1</code> = never lose capacity, add one at a time. The rollout <b>waits for readiness</b> before continuing — a broken image with a failing readiness probe stalls the rollout instead of taking you down.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Rollout "stuck at 3/5 updated" for 10 minutes is almost always the new Pods failing readiness. <code>kubectl describe pod</code> + <code>kubectl logs</code> the new one — do not <code>rollout undo</code> blindly, read first.</p></div>',
    try: [['📖 K8s applications guide', '../kubernetes/APPLICATIONS.md', 'o']] },
  { html: '<p><b>StatefulSet</b> for Pods that are <i>not</i> interchangeable: stable names (<code>db-0</code>, <code>db-1</code>), stable per-Pod storage that survives rescheduling (<code>volumeClaimTemplates</code>), ordered rollout. Pair it with a <b>headless Service</b> (<code>clusterIP: None</code>) for per-Pod DNS.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Deployment Pods are interchangeable taxi drivers. StatefulSet Pods are numbered train carriages — carriage 2 must be carriage 2, with its own contents, in order.</p></div>',
    try: [['🔬 Lab: StatefulSet', '../kubernetes/manifests/05-statefulset.yaml', 'o']] },
  { html: '<p><b>Services</b> give a stable virtual IP + DNS name in front of a changing set of Pods. Types: <code>ClusterIP</code> (in-cluster only), <code>NodePort</code> (a port on every node), <code>LoadBalancer</code> (cloud LB). <b>Ingress</b> (or Gateway API) does host/path HTTP routing + TLS in front of many Services — implemented by a controller (ingress-nginx, Traefik).</p>' +
    '<pre><code>internet ─▶ LoadBalancer ─▶ Ingress controller ─▶ /api  ▶ Service api\n                                              └▶ /     ▶ Service web</code></pre>',
    try: [['🔬 Lab: Ingress', '../kubernetes/manifests/04-ingress.yaml', 'o']] },
  { html: '<p>Expert view: <b>Jobs</b> (run to completion, retries via <code>backoffLimit</code>) and <b>CronJobs</b> (Jobs on a schedule, mind <code>concurrencyPolicy</code> and missed-start deadlines). <b>DaemonSets</b> put one Pod per node (log shippers, CNI, node exporters). Progressive delivery — <b>canary</b> and <b>blue/green</b> — with Argo Rollouts or a service mesh; plain-K8s blue/green is two Deployments and a Service selector flip.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>A CronJob that "sometimes runs twice" usually has <code>concurrencyPolicy: Allow</code> (the default) and a slow job overlapping its next tick. Set <code>Forbid</code> or <code>Replace</code>.</p></div>',
    try: [['🔬 Lab: blue/green cutover', '../kubernetes/manifests/10-blue-green.yaml', 'o']] } ],
  quiz: [
    { q: 'Why deploy a Deployment instead of a bare Pod?', opts: ['Pods cannot have containers', 'Deployment -> ReplicaSet gives self-healing, declarative scaling, rolling updates and instant rollback', 'Pods cost more', 'Deployments skip the scheduler'], ok: 1, why: 'A bare Pod that dies stays dead. The Deployment/ReplicaSet controllers recreate, roll and roll back.' },
    { q: 'A rollout is stuck halfway for 10 minutes. First move?', opts: ['kubectl rollout undo immediately', 'Describe + log the new (not-Ready) Pods — the rollout is gated on their readiness probe; find why they fail', 'Delete the Deployment', 'Scale to zero and back'], ok: 1, why: 'RollingUpdate waits for new Pods to pass readiness before retiring old ones. Stuck = new Pods unhealthy. Read logs first; undo is the fix only after you know it is a bad build.' },
    { q: 'What does a StatefulSet give that a Deployment does not?', opts: ['Faster Pods', 'Stable network identity, stable per-Pod persistent storage across reschedules, and ordered rollout', 'Automatic backups', 'Cheaper storage'], ok: 1, why: 'Deployments treat Pods as fungible. StatefulSets give sticky identity + sticky volumes — needed for databases, queues, quorum systems.' } ]
},

{ num: 6, emoji: '🔐', title: 'Config, Storage & Security — the platform contract', layer: 'Platform',
  tagline: 'Config outside the image, why a Secret is not encryption, the storage lifecycle, RBAC, and default-deny networking.',
  apps: ['🔑 secrets management', '💾 persistent volumes', '🛡️ zero-trust clusters'],
  levels: [
  { html: '<p><b>ConfigMap</b> and <b>Secret</b> keep configuration out of the image, so one image runs in every environment. Inject as env vars (<code>envFrom</code>) or mounted files (a volume — updates live, after a delay). Env vars are read <b>once at start</b>: editing a ConfigMap does not restart Pods — you need <code>kubectl rollout restart</code>.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Helm/Kustomize set a <code>checksum/config</code> annotation on the Pod template = hash of the ConfigMap, so changing config <i>does</i> trigger a rollout automatically.</p></div>',
    try: [['🔬 Lab: ConfigMap + Secret', '../kubernetes/manifests/03-config-secret.yaml', 'o']] },
  { html: '<p>A <b>Secret is base64, not encryption.</b> Anyone with <code>get secret</code> RBAC, or read access to etcd, sees the value. Real clusters add: <b>encryption at rest</b> for etcd, an <b>external manager</b> (External Secrets Operator, Vault, cloud secret store) syncing in, and tight RBAC so almost nobody can <code>get secret</code>.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>base64 is writing the password backwards on the envelope. It is not sealed — it is just less obvious at a glance.</p></div>',
    try: [['📖 Security deep dive', '../kubernetes/DEEP_DIVE.md', 'o']] },
  { html: '<p>Storage lifecycle: a <b>StorageClass</b> describes a kind of disk; a <b>PersistentVolumeClaim</b> (PVC) is a request; the provisioner creates a <b>PersistentVolume</b> (PV) and binds them; the <b>CSI</b> driver attaches/mounts it to the node. <code>reclaimPolicy: Retain</code> vs <code>Delete</code> decides whether your data survives deleting the PVC. <code>ReadWriteOnce</code> (one node) is the common access mode; <code>ReadWriteMany</code> needs NFS/CephFS-style storage.</p>',
    try: [['🔬 Lab: volumeClaimTemplates', '../kubernetes/manifests/05-statefulset.yaml', 'o']] },
  { html: '<p><b>RBAC</b>: a <code>Role</code> (namespaced) or <code>ClusterRole</code> lists allowed verbs on resources; a <code>RoleBinding</code> grants it to a user, group, or <b>ServiceAccount</b>. Least privilege: give each workload its own ServiceAccount with exactly the access it needs (often none — set <code>automountServiceAccountToken: false</code>).</p>' +
    '<pre><code>kubectl auth can-i list pods --as=system:serviceaccount:default:checkout\n# yes\nkubectl auth can-i delete pods --as=system:serviceaccount:default:checkout\n# no</code></pre>',
    try: [['🔬 Lab: RBAC', '../kubernetes/manifests/07-rbac.yaml', 'o']] },
  { html: '<p>Expert view: by default <b>every Pod can reach every Pod</b>. A default-deny <b>NetworkPolicy</b> plus explicit allows (needs a policy-enforcing CNI — Calico, Cilium) closes the lateral-movement path. Add <b>Pod Security Admission</b> (<code>restricted</code>: non-root, no privilege escalation, drop ALL caps, seccomp <code>RuntimeDefault</code>), image <b>signing + admission</b> (Sigstore/cosign + Kyverno), and runtime detection (Falco).</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Applying NetworkPolicies on a CNI that ignores them is a silent no-op — everything still passes. Always verify with an actual blocked <code>curl</code> between namespaces.</p></div>',
    try: [['🔬 Lab: NetworkPolicy', '../kubernetes/manifests/08-networkpolicy.yaml', 'o']] } ],
  quiz: [
    { q: 'You edited a ConfigMap consumed as env vars. Why did nothing change?', opts: ['ConfigMaps are immutable', 'Env vars are injected once at Pod start — you must roll the Pods (rollout restart) or mount the config as a volume', 'You need to restart the API server', 'The change needs 24h to propagate'], ok: 1, why: 'Env-var injection happens at container start. Volume-mounted ConfigMaps update in place (with a kubelet sync delay); env vars do not.' },
    { q: 'Is a Kubernetes Secret encrypted?', opts: ['Yes, AES-256 by default', 'No — it is base64-encoded; protection comes from etcd encryption-at-rest + RBAC + an external secret manager', 'Yes, if the name starts with secret-', 'Only in production clusters'], ok: 1, why: 'Secrets are base64 in etcd. Without encryption-at-rest and tight RBAC, anyone with get secret or etcd access reads them plainly.' },
    { q: 'Default Pod-to-Pod networking in a fresh cluster is:', opts: ['Deny all', 'Allow all — any Pod can reach any Pod/Service until a NetworkPolicy restricts it (and the CNI enforces it)', 'Only same-namespace', 'Only via Services'], ok: 1, why: 'Flat allow-all by default. Default-deny + explicit allows, enforced by a capable CNI, is what gives you segmentation.' } ]
},

{ num: 7, emoji: '📈', title: 'Scaling & Self-Healing — requests, probes, autoscaling', layer: 'Operations',
  tagline: 'Requests vs limits and the OOMKill, QoS, the three probes and how they cause outages, HPA/VPA/KEDA, PDBs.',
  apps: ['📊 traffic-spike autoscaling', '💰 cost control', '🩹 automatic recovery'],
  levels: [
  { html: '<p><b>Requests</b> = what the scheduler reserves for a container (and the basis for autoscaling %). <b>Limits</b> = the hard ceiling. Exceed a memory limit → <b>OOMKilled</b>. Exceed a CPU limit → <b>throttled</b> (not killed) — which looks exactly like "the app got slow".</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>Request = the seats you reserve on the train. Limit = the aisle you are allowed to stand in. Too many standing passengers for you (memory) and you are thrown off; too slow through the aisle (CPU) and you just move at a crawl.</p></div>',
    try: [['📖 K8s prod scenarios (OOM, throttle)', '../kubernetes/PROD_SCENARIOS.md', 'o']] },
  { html: '<p><b>QoS classes</b> fall out of requests/limits: <code>Guaranteed</code> (requests == limits everywhere), <code>Burstable</code> (some requests set), <code>BestEffort</code> (none). Under node memory pressure the kubelet <b>evicts BestEffort first, Guaranteed last</b>. Setting requests is how you avoid being the first Pod thrown overboard.</p>',
    try: [['📖 Scaling deep dive', '../kubernetes/DEEP_DIVE.md', 'o']] },
  { html: '<p>Three probes, three jobs. <b>startupProbe</b>: "still booting?" — holds the others off, so a slow JVM start does not need a long liveness delay. <b>livenessProbe</b>: "wedged? restart me" — keep it cheap and dependency-free. <b>readinessProbe</b>: "put me in the load balancer?" — this one may check dependencies; failing it just removes traffic, no restart.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Classic outage: a liveness probe that calls the database. The DB blips, every Pod fails liveness, every Pod restarts at once, the stampede keeps the DB down. Liveness must not depend on anything external.</p></div>',
    try: [['🔬 Lab: full probe set + PDB', '../kubernetes/manifests/09-pdb-probes.yaml', 'o']] },
  { html: '<p><b>HPA</b> adds/removes Pods to hit a target metric (CPU %, memory %, or custom/external — RPS, queue depth via KEDA). It needs <b>requests set</b> (the % is of the request) and metrics-server. <b>VPA</b> right-sizes requests/limits (do not run it in <code>Auto</code> mode alongside HPA on the same metric). <b>Cluster Autoscaler</b> / Karpenter add <i>nodes</i> when Pods cannot be scheduled.</p>' +
    '<pre><code>3 layers: HPA (more Pods) → nodes fill up → Cluster Autoscaler (more nodes)\n          ↑ needs resource requests to make any decision</code></pre>',
    try: [['🔬 Lab: HPA with behavior', '../kubernetes/manifests/06-hpa.yaml', 'o']] },
  { html: '<p>Expert view: protect availability during <b>voluntary</b> disruptions with a <b>PodDisruptionBudget</b> (<code>minAvailable: 80%</code> blocks a node drain that would go too far). Spread with <b>topologySpreadConstraints</b> / anti-affinity so one node or zone loss is survivable. Graceful shutdown: <code>SIGTERM</code> → <code>preStop</code> sleep (let the LB stop sending) → drain in-flight → exit before <code>terminationGracePeriodSeconds</code>.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>502s during every deploy? Add a <code>preStop</code> sleep of ~10s and make sure the app handles SIGTERM. The Pod is removed from Endpoints and the LB drains before the process dies.</p></div>',
    try: [['📖 Graceful shutdown & PDBs', '../kubernetes/PROD_SCENARIOS.md', 'o']] } ],
  quiz: [
    { q: 'A container hits its CPU limit. What happens?', opts: ['OOMKilled', 'It is CPU-throttled — still running, just slower (often misdiagnosed as a slow app)', 'The node reboots', 'Nothing, limits are advisory'], ok: 1, why: 'Memory over limit = OOMKill. CPU over limit = throttling. High latency + high cpu.cfs_throttled time = raise the limit, not the replica count.' },
    { q: 'Why must a liveness probe be dependency-free?', opts: ['It runs too often otherwise', 'If it checks a shared dependency (DB), one blip fails liveness on every Pod at once → mass simultaneous restart → stampede', 'Probes cannot make network calls', 'It would slow the app'], ok: 1, why: 'Liveness failure = restart. Coupling it to a shared dependency turns a dependency blip into a cluster-wide restart storm. Put dependency checks in readiness.' },
    { q: 'HPA is not scaling your Deployment. Most common root cause?', opts: ['maxReplicas too high', 'No resource requests set — HPA target utilisation is a percentage of the request, so with no request there is nothing to compute', 'Too many nodes', 'The Service is ClusterIP'], ok: 1, why: 'No requests -> no utilisation ratio -> HPA cannot act (and the scheduler is guessing too). metrics-server missing is the other usual cause.' } ]
},

{ num: 8, emoji: '🔥', title: 'SRE & Production Ops — running it when it breaks', layer: 'Operations',
  tagline: 'SLIs/SLOs/error budgets, the four golden signals, incident command, rollout strategy, and blameless postmortems.',
  apps: ['📟 on-call rotations', '📉 the status page', '🧯 the 3 a.m. page'],
  levels: [
  { html: '<p><b>SRE</b> is operations run like a software problem: define reliability numerically, spend an <b>error budget</b>, automate the toil. An <b>SLI</b> is a measurement (successful requests / total). An <b>SLO</b> is the target (99.9% over 30 days). The <b>error budget</b> is <code>1 − SLO</code> — the amount of failure you are <i>allowed</i>, and can spend on risk.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>The error budget is a monthly data allowance. Under budget → ship features fast, take risks. Blown the budget → freeze features, everyone works on reliability until it recovers.</p></div>',
    try: [['📖 SRE Beginner Guide', '../sre/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p>The <b>four golden signals</b> to graph for every service: <b>Latency</b> (and split success vs error latency), <b>Traffic</b> (RPS), <b>Errors</b> (rate + which), <b>Saturation</b> (how full — CPU, memory, queue depth, connection pool). If you can only build four dashboards, build these.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>Alert on <b>symptoms</b> (SLO burn rate, error ratio), not causes (CPU 90%). "CPU is high" at 3 a.m. with users happy is a false page; "checkout error rate 8%" is a real one.</p></div>',
    try: [['📖 Golden signals & alerting', '../sre/BEGINNER_GUIDE.md', 'o']] },
  { html: '<p>Running an <b>incident</b>: (1) declare it — a named channel, a doc; (2) assign roles — Incident Commander (decides), Comms (updates stakeholders/status page), Ops (hands on keyboard); (3) <b>mitigate before diagnosing</b> — roll back, fail over, shed load, feature-flag off; (4) only then find root cause; (5) blameless <b>postmortem</b> with action items that have owners and dates.</p>' +
    '<div class="analogy"><span class="lbl">🎯 Analogy</span><p>ER triage: stop the bleeding (mitigate) before you order the MRI (root cause). A patient stable on the table buys you time to investigate safely.</p></div>',
    try: [['📖 SRE prod challenge playbooks', '../sre/PROD_CHALLENGES.md', 'o']] },
  { html: '<p><b>Rollout strategies</b> as risk controls: <b>rolling</b> (default, gradual), <b>blue/green</b> (instant switch + instant rollback, 2× cost), <b>canary</b> (1% → 10% → 50% → 100%, watch SLOs at each step, auto-halt on regression). Always pair with <b>feature flags</b> so "deploy" and "release" are separate events — dark-launch code, then turn it on for 1% of users.</p>' +
    '<pre><code>deploy v2 to 5% ─▶ error rate +0.1%? hold. +3%? auto-rollback.\n            ─▶ clean for 15 min ─▶ 25% ─▶ 50% ─▶ 100%</code></pre>',
    try: [['🔬 Lab: blue/green', '../kubernetes/manifests/10-blue-green.yaml', 'o']] },
  { html: '<p>Expert view: <b>multi-window burn-rate alerting</b> (fast burn = page now, slow burn = ticket), <b>capacity planning</b> from traffic forecasts + headroom for failover (N+1, or lose a zone and survive), <b>toil budget</b> (cap manual ops at ~50% so automation actually gets built), <b>chaos/GameDays</b> to test the assumptions, and <b>DORA</b> metrics (deploy frequency, lead time, change-fail rate, MTTR) to know if you are improving.</p>' +
    '<div class="reallife"><span class="lbl">🛠️ In your daily work</span><p>The most valuable SRE habit: after every incident, one concrete, owned, dated action item that makes that exact failure impossible or self-healing — not "be more careful".</p></div>',
    try: [['📖 SRE interview Q&A', '../sre/INTERVIEW_QA.md', 'o']] } ],
  quiz: [
    { q: 'What is an error budget?', opts: ['Money set aside for outages', 'The allowed amount of unreliability: 1 − SLO. Spend it on release velocity; freeze features when it runs out', 'The number of on-call engineers', 'The count of open bugs'], ok: 1, why: 'A 99.9% SLO gives a 0.1% budget (~43 min/month). Healthy budget = ship fast; exhausted budget = reliability freeze. It aligns dev and ops incentives.' },
    { q: 'During a live incident, what comes first?', opts: ['Find the root cause', 'Mitigate — roll back / fail over / shed load / flag off — to stop user impact; root cause comes after', 'Write the postmortem', 'Page the CTO'], ok: 1, why: 'Stop the bleeding first. A mitigated system is stable enough to investigate without pressure. Root-causing a live outage wastes the minutes that matter.' },
    { q: 'Best practice for what to alert (page) on?', opts: ['Any resource above 80%', 'User-visible symptoms and SLO burn rate — not raw causes like high CPU, which page falsely when users are fine', 'Every ERROR log line', 'Node restarts only'], ok: 1, why: 'Symptom-based alerting (error ratio, latency SLO, burn rate) pages when users hurt. Cause-based alerts (CPU) generate noise and alert fatigue.' } ]
}

];
