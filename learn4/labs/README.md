# Part 4 · labs — runnable configs

> Standard tools, at production depth. Every file here is validated in CI
> (`.github/workflows/lab-tests.yml`).

Landing alongside chapters 2–16: a 3-tier VPC Terraform module, an IAM
least-privilege policy example, a Vault dynamic-secrets config, a cert-manager
Certificate + ClusterIssuer, and a CIS-benchmark scan config.

Until then, this directory is intentionally a placeholder so links resolve.
