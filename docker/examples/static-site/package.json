{
  "name": "static-site-demo",
  "version": "1.0.0",
  "private": true,
  "description": "Placeholder manifest for the multi-stage static-site Dockerfile demo",
  "scripts": {
    "build": "mkdir -p out && cp -r public/* out/"
  }
}
