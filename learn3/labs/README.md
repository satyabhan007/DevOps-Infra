# Part 3 · labs — runnable configs

> Standard tools, at production depth. Every file here is validated in CI
> (`.github/workflows/lab-tests.yml`).

Landing alongside chapters 2–16: a minimal CRD + controller skeleton, an Argo CD
Application + Kustomize overlays, a Kyverno policy set, an Istio VirtualService
example, and Karpenter/Cluster-Autoscaler provisioner configs.

Until then, this directory is intentionally a placeholder so links resolve.
